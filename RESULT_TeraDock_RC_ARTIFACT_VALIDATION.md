# TeraDock RC Artifact Validation Result

Validation date: 2026-06-12 23:37:50 +09:00

## Scope

Target repository: `ardnico/TeraDock`

Target release path:

- Artifact-only validation before `v0.1.0-rc1`.
- No production `v0.1.0` tag push.
- No GitHub Release creation.
- No feature work.

Human validation input status:

- The provided validation log block contained only the placeholder text:
  `ここに検証ログを貼る`.
- No human smoke-test transcript was available for Linux install tests, Windows
  installer tests, TUI manual smoke, or security log review.

## Workflow Run Information

Observed through the public GitHub Actions API:

- Workflow: `Release`
- Run ID: `27421916147`
- Run number: `14`
- URL: `https://github.com/ardnico/TeraDock/actions/runs/27421916147`
- Event: `workflow_dispatch`
- Branch: `main`
- Head SHA: `57dea3fda57c793ab182a1c59598231b6250d1f2`
- Created: `2026-06-12T14:25:24Z`
- Updated: `2026-06-12T14:28:59Z`
- Status: `completed`
- Conclusion: `success`

Jobs:

| Job | Status | Conclusion | Notes |
| --- | --- | --- | --- |
| `Build (ubuntu-latest)` | `completed` | `success` | Linux build and packaging job completed. |
| `Build (windows-latest)` | `completed` | `success` | Windows build and installer job completed. |
| `Create GitHub Release` | `completed` | `skipped` | Expected for `workflow_dispatch`; no release page was created by this run. |

Remote tag/release safety check:

- Remote tags observed: `v1.0.1`, `v1.0.2`
- `v0.1.0` was not observed in remote tags.
- GitHub Releases API returned no release entries.

## Artifact List

Observed Actions artifact groups for run `27421916147`:

| Artifact group | Artifact ID | Size | Created | Expires | Expired |
| --- | ---: | ---: | --- | --- | --- |
| `linux-artifacts` | `7594202215` | `7719942` bytes | `2026-06-12T14:27:32Z` | `2026-09-10T14:25:26Z` | `false` |
| `windows-artifacts` | `7594227110` | `3467706` bytes | `2026-06-12T14:28:34Z` | `2026-09-10T14:25:26Z` | `false` |

Artifact contents were not downloaded in this pass. The GitHub artifact download
endpoint returned `401 Requires authentication` from this environment.

Expected artifact files from the release workflow and docs:

- `td-<version>-linux-x86_64.tar.gz`
- Debian package generated by `cargo deb`
- RPM package generated by `cargo generate-rpm`
- `td-<version>-windows-x86_64-setup.exe`
- `SHA256SUMS-linux-x86_64`
- `SHA256SUMS-windows-x86_64`

The exact file names inside the artifact zips were not verified because the
artifact download was not available without authentication and no pasted
artifact listing was provided.

## Checksum Result

Status: NOT VERIFIED

- Workflow metadata confirms that `linux-artifacts` and `windows-artifacts`
  were uploaded.
- The artifact groups are expected to include `SHA256SUMS-linux-x86_64` and
  `SHA256SUMS-windows-x86_64` based on `.github/workflows/release.yml`.
- No checksum command output was provided.
- Artifact contents could not be downloaded from this environment.

Required before rc1:

- Download both artifact groups from run `27421916147`.
- Verify Linux checksums with `sha256sum -c SHA256SUMS-linux-x86_64`.
- Verify the Windows installer hash against `SHA256SUMS-windows-x86_64`.

## Linux tar.gz Result

Status: NOT VERIFIED

| Check | Result | Evidence |
| --- | --- | --- |
| Checksum | Not verified | No checksum output supplied. |
| Extract | Not verified | No `tar -xzf` output supplied. |
| `td --help` | Not verified | No smoke-test output supplied. |
| `td doctor` | Not verified | No smoke-test output supplied. |
| `td init --with-samples` | Not verified | No smoke-test output supplied. |
| `td profile list` | Not verified | No smoke-test output supplied. |
| `td config keys` | Not verified | No smoke-test output supplied. |
| `td recent --json` | Not verified | No smoke-test output supplied. |

## deb Result

Status: NOT VERIFIED

| Check | Result | Evidence |
| --- | --- | --- |
| Install | Not verified | No `apt install ./<artifact>.deb` output supplied. |
| Smoke test | Not verified | No installed `td` command output supplied. |
| Uninstall | Not verified | No `apt remove td` output supplied. |

## rpm Result

Status: NOT VERIFIED

| Check | Result | Evidence |
| --- | --- | --- |
| Install | Not verified | No `dnf install ./<artifact>.rpm` output supplied. |
| Smoke test | Not verified | No installed `td` command output supplied. |
| Uninstall | Not verified | No `dnf remove td` output supplied. |

## Windows Installer Result

Status: NOT VERIFIED

| Check | Result | Evidence |
| --- | --- | --- |
| Install | Not verified | No installer transcript supplied. |
| PATH update | Not verified | No installed-shell `td.exe` output supplied. |
| Smoke test | Not verified | No `td.exe --help`, `doctor`, `init --with-samples`, `profile list`, or `recent --json` output supplied. |
| Uninstall | Not verified | No uninstall transcript supplied. |

## TUI Manual Smoke Result

Status: NOT VERIFIED

| Check | Result | Evidence |
| --- | --- | --- |
| `td ui` starts | Not verified | No manual smoke log supplied. |
| Search/filter/details | Not verified | No manual smoke log supplied. |
| `s` interactive SSH session | Not verified | No manual smoke log supplied. |
| TUI returns after SSH exit | Not verified | No manual smoke log supplied. |
| Non-SSH profile handling | Not verified | No manual smoke log supplied. |
| Critical confirmation | Not verified | No manual smoke log supplied. |
| Non-TTY behavior | Not verified | No manual smoke log supplied. |

## Security Result

Status: NOT VERIFIED

| Check | Result | Evidence |
| --- | --- | --- |
| Secrets/passwords/auth args/full SSH command strings absent from logs | Not verified | No validation logs supplied. Public workflow metadata did not include command stdout. |
| `td recent --json` avoids excessive information | Not verified | No command output supplied. |
| FTP insecure acknowledgement | Not verified | No command output supplied. |
| Critical confirmation | Not verified | No manual smoke or CLI transcript supplied. |

The provided input itself did not contain secrets, passwords, auth args, private
key paths, or full command strings, but that is not sufficient evidence for the
artifact validation gate.

## Documentation and Artifact Routing Check

Status: PARTIAL

Verified locally:

- `README.md`, `RELEASE_NOTES_0.1.0.md`, `RELEASE_CHECKLIST.md`, and
  `docs/release-artifact-validation.md` all document the expected artifact-only
  workflow path and the expected artifact groups:
  - `linux-artifacts`
  - `windows-artifacts`
- The workflow uploaded artifact groups with those exact names.
- The workflow skipped `Create GitHub Release` for the `workflow_dispatch` run.
- README and release notes name the expected production tarball and Windows
  installer:
  - `td-0.1.0-linux-x86_64.tar.gz`
  - `td-0.1.0-windows-x86_64-setup.exe`

Not verified:

- Exact file names inside `linux-artifacts` and `windows-artifacts`.
- Exact deb/rpm file names.
- Checksum file contents.
- Whether the downloaded artifact names match the docs in a human validation
  environment.

## Go / No-Go Decision

Decision: `NO-GO: 修正が必要`

This is a release-readiness no-go caused by missing validation evidence, not by
a confirmed code or packaging defect.

Reasons:

- PASS: Latest observed `workflow_dispatch` Release run completed
  successfully.
- PASS: Linux and Windows artifact groups were uploaded.
- PASS: GitHub Release job was skipped for the manual artifact-only run.
- PASS: No remote `v0.1.0` tag was observed.
- PASS: No GitHub Release entries were returned by the public Releases API.
- BLOCKED: Artifact contents and exact artifact file names were not verified.
- BLOCKED: Checksum verification output was not provided.
- BLOCKED: Linux tar.gz smoke test output was not provided.
- BLOCKED: Neither deb nor rpm install smoke output was provided.
- BLOCKED: Windows installer smoke output was not provided.
- BLOCKED: TUI manual smoke output was not provided.
- BLOCKED: Security log review output was not provided.
- PARTIAL: README/release notes match expected workflow artifact routing, but
  exact downloaded file names were not verified.

The release should not proceed to `v0.1.0-rc1` until the missing human
validation evidence is collected and reviewed.

## Changes Made

- Added this result file:
  `RESULT_TeraDock_RC_ARTIFACT_VALIDATION.md`

No runtime code, workflow, packaging script, installer metadata, README,
release notes, or validation-guide changes were made.

## Unverified Items

- Artifact zip contents.
- Linux checksum verification.
- Windows checksum verification.
- Linux tar.gz extraction and CLI smoke.
- deb install, smoke, and uninstall.
- rpm install, smoke, and uninstall.
- Windows installer install, PATH behavior, smoke, and uninstall.
- TUI manual smoke.
- Runtime security logs and `td recent --json` output.
- Exact deb/rpm package names.

## Required Human Steps Before `v0.1.0-rc1`

1. Open `https://github.com/ardnico/TeraDock/actions/runs/27421916147`.
2. Download `linux-artifacts` and `windows-artifacts`.
3. Record the exact file list inside each artifact zip.
4. Run Linux checksum verification:

   ```bash
   sha256sum -c SHA256SUMS-linux-x86_64
   ```

5. Run the Linux tar.gz smoke test from a clean directory:

   ```bash
   tar -xzf <artifact>.tar.gz
   cd td-<version>-linux-x86_64
   ./td --help
   ./td doctor
   ./td init --with-samples
   ./td profile list
   ./td config keys
   ./td recent --json
   ```

6. Run at least one Linux package install smoke test. Prefer both if
   environments are available:

   ```bash
   sudo apt install ./<artifact>.deb
   td --help
   td doctor
   td init --with-samples
   td profile list
   td recent --json
   sudo apt remove td
   ```

   ```bash
   sudo dnf install ./<artifact>.rpm
   td --help
   td doctor
   td init --with-samples
   td profile list
   td recent --json
   sudo dnf remove td
   ```

7. Run Windows checksum verification and installer smoke:

   ```powershell
   Get-FileHash .\td-<version>-windows-x86_64-setup.exe -Algorithm SHA256
   Get-Content .\SHA256SUMS-windows-x86_64
   td.exe --help
   td.exe doctor
   td.exe init --with-samples
   td.exe profile list
   td.exe recent --json
   ```

8. Confirm installer PATH behavior and uninstall behavior on Windows.
9. Run TUI manual smoke with disposable profiles/hosts:
   - `td ui`
   - `/` search
   - type/group/danger/tag/query filters
   - details view
   - `s` on an SSH profile
   - SSH exit returns to TUI
   - non-SSH profile does not open SSH
   - critical profile typed confirmation
   - non-TTY `td ui` behavior
10. Review logs for secret/password/auth arg/private key path/full command
    string exposure.
11. Paste the validation transcript back into this result file or rerun this
    review with the transcript available.

## Revalidation Procedure

After the missing evidence is collected:

1. Update the result tables above from `NOT VERIFIED` to `PASS` or `FAIL`.
2. If every required gate passes, change the decision to:
   `GO: v0.1.0-rc1へ進める`
3. If only a clearly scoped environment gap remains, change the decision to:
   `CONDITIONAL GO: 条件付きでrc1へ進める`
4. If any actual artifact, install, TUI return, or security failure is observed,
   keep:
   `NO-GO: 修正が必要`

Priority if failures are found:

1. Checksum mismatch, missing artifact, or wrong artifact naming.
2. `td --help` or `td init --with-samples` failure from extracted/installed
   artifact.
3. Package/installer install, PATH, or uninstall failure.
4. TUI failure to return after SSH exit.
5. Secret/password/auth arg/private key path/full command string exposure.
6. README/release notes mismatch with downloaded artifact names.

## Tests Run

Because this pass only added a validation result document, the required minimum
validation is:

```bash
git diff --check
```

Result: passed.
