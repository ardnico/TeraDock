name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build:
    name: Build (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Rust cache
        uses: Swatinem/rust-cache@v2

      - name: Resolve version
        id: version
        shell: bash
        run: echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Install rpm tooling and libudev
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y rpm libudev-dev pkg-config

      - name: Build release binary
        run: cargo build -p td --release --locked

      # Linux packaging
      - name: Install cargo-deb
        if: runner.os == 'Linux'
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-deb

      - name: Install cargo-rpm
        if: runner.os == 'Linux'
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-rpm

      - name: Build deb package
        if: runner.os == 'Linux'
        run: cargo deb -p td --no-build

      - name: Build rpm package
        if: runner.os == 'Linux'
        working-directory: crates/cli
        run: cargo rpm

      - name: Build tar.gz archive
        if: runner.os == 'Linux'
        run: packaging/linux/package-tar.sh "${{ steps.version.outputs.version }}"

      # Windows packaging
      - name: Install Inno Setup
        if: runner.os == 'Windows'
        run: choco install innosetup --no-progress

      - name: Build Windows installer
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          & "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" `
            /DMyAppVersion="${{ steps.version.outputs.version }}" `
            "packaging\windows\td.iss"

      - name: Upload artifacts (Linux)
        if: runner.os == 'Linux'
        uses: actions/upload-artifact@v4
        with:
          name: linux-artifacts
          path: |
            dist/td-*-linux-x86_64.tar.gz
            target/debian/*.deb
            **/target/rpm/*.rpm

      - name: Upload artifacts (Windows)
        if: runner.os == 'Windows'
        uses: actions/upload-artifact@v4
        with:
          name: windows-artifacts
          path: |
            dist/td-*-windows-x86_64-setup.exe

  release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-assets

      - name: Create/Update release and upload assets
        uses: softprops/action-gh-release@v2
        with:
          files: release-assets/**/*
          fail_on_unmatched_files: true
          generate_release_notes: true
